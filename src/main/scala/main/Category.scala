package main

/** Groups similar types together
  * @example Math questions would include questions which can theorem, definition, proof.
  *          There can be various math subjects, and categories would enable quick setup for them
  * @param name name
  * @param types collections of types that become available to all decks in topic/subtopic
  */
class Category(name: String, types: Seq[Type] = Seq.empty) {

}
